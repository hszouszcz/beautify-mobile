# Copy to `.env` (committed default) or `.env.local` (gitignored override).
# EXPO_PUBLIC_* vars are inlined at build time — restart Metro with `--clear`
# after changing them.

# Base origin of the Beautify DRF API, including the `/api` prefix.
EXPO_PUBLIC_API_URL=https://api.beautify.local/api
